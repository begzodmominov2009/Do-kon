// Stock count at or below which a product is flagged as "running low" —
// ProductCard, the product detail page's availability tag, and its warning
// strip all key off this single value.
export const LOW_STOCK_THRESHOLD = 5;
